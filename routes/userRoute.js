import express from "express";

const router = express.Router();

router.get("/:id", () => {});

router.post("/refresh", () => {});

router.post("/register", () => {});

router.post("/login", () => {});

export default router;
